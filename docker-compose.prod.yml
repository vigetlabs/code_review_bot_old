version: '3'
services:
  web:
    command: code_review_bot -p 80
    build:
      context: .
      dockerfile: Dockerfile.prod
    environment:
      - SLACK_CHANNEL=#code-review
      - WEBHOOK_URL=http:://crbot.vigetx.com/github_event
    ports:
      - "80:80"
